const ADAPTERS=[{city:'Ottawa',adapter:'ArcGIS/Open Data catalogue + traffic JSON',mode:'controlled-server-side',status:'contract-ready'},{city:'Toronto',adapter:'Toronto Open Data / CKAN-style resources',mode:'controlled-server-side',status:'contract-ready'},{city:'Melbourne',adapter:'City/data.gov.au open-data resources',mode:'controlled-server-side',status:'contract-ready'}];
function renderAdapters(){const e=document.getElementById('adapterRegistry');if(e)e.innerHTML=ADAPTERS.map(x=>'<div class="candidate"><b>'+hEsc(x.city)+'</b><div class="small">'+hEsc(x.adapter)+'</div><div class="small">'+hEsc(x.mode)+' · <b>'+hEsc(x.status)+'</b></div></div>').join('')}
