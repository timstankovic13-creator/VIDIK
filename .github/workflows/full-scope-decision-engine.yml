name: Full-Scope Decision Engine
on:
  push:
    branches: [main]
    paths:
      - 'src/full-scope/**'
      - 'tests/full-scope-decision-engine.test.js'
      - '.github/workflows/full-scope-decision-engine.yml'
jobs:
  contract:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-node@v5
        with:
          node-version: 24
      - run: node tests/full-scope-decision-engine.test.js
