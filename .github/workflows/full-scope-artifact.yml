name: Full-Scope Decision Artifact
on:
  push:
    branches: [main]
    paths:
      - 'src/full-scope/**'
      - 'tests/full-scope-artifact.test.js'
      - '.github/workflows/full-scope-artifact.yml'
jobs:
  contract:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - run: node tests/full-scope-artifact.test.js
