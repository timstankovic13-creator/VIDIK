'use strict';
const finite=x=>Number.isFinite(Number(x));const positive=x=>finite(x)&&Number(x)>0;const text=x=>String(x??'').trim();function canonicalUnits(v){const x=text(v).toLowerCase();return x||null;}
function stableCandidateId(raw={}){if(text(raw.id))return text(raw.id);const basis=[text(raw.name),text(raw.jurisdiction),...(raw.sourceIds||[])].join('|').toLowerCase().trim();const slug=basis.replace(/[^a-z0-9]+/g,'-').replace(/^-+|-+$/g,'').slice(0,100);return `candidate:${slug||'unnamed'}`;}
function normalizeCandidate(raw={}){return {id:stableCandidateId(raw),name:text(raw.name)||'Unnamed candidate',sourceIds:[...new Set((raw.sourceIds||raw.sourceId?[...(raw.sourceIds||[]),...(raw.sourceId?[raw.sourceId]:[])]:[]).map(text).filter(Boolean))],jurisdiction:text(raw.jurisdiction)||'unknown',discoveryOnly:raw.discoveryOnly!==false,leadOnly:raw.leadOnly!==false,effectsImported:raw.effectsImported===true||raw.causalEffectImported===true,evidence:Array.isArray(raw.evidence)?raw.evidence:[],parameters:Array.isArray(raw.parameters)?raw.parameters:[],implementation:raw.implementation||null,relevance:finite(raw.relevance)?Number(raw.relevance):0};}
function classifyEvidence(evidence={}){const design=text(evidence.design).toLowerCase();const verified=evidence.verification?.status==='verified'||evidence.verified===true;const local=evidence.jurisdictionLocal===true||evidence.local===true;const negated=/(^|\b)(not|non|no|without|failed to show|fails to show|insufficient)\s+(a\s+|an\s+)?(randomized|rct|quasi-experimental|causal|natural experiment|instrumental variable|difference-in-differences|regression discontinuity)/i.test(design);const causal=!negated&&['rct','randomized','quasi-experimental','difference-in-differences','regression discontinuity','natural experiment','instrumental variable'].some(k=>design.includes(k));return {id:text(evidence.id),sourceId:text(evidence.sourceId),design:design||'unknown',causal,verified,local,admissible:causal&&verified,importedEffect:evidence.importedEffect===true||evidence.causalEffectImported===true};}
function evidenceState(candidate){const evidence=(candidate.evidence||[]).map(classifyEvidence);const admissible=evidence.filter(e=>e.admissible&&!e.importedEffect);const independent=new Set(admissible.map(e=>e.sourceId).filter(Boolean));const local=admissible.filter(e=>e.local);return {evidence,admissibleCount:admissible.length,independentSourceCount:independent.size,locallyAdmissibleCount:local.length,importedEffectCount:evidence.filter(e=>e.importedEffect).length,hasIndependentCausalEvidence:admissible.length>0&&independent.size>=2,hasLocalCausalEvidence:local.length>0};}
function parameterState(candidate){const params=(candidate.parameters||[]).map(p=>({id:text(p.id),effect:Number(p.effect),effectUnit:canonicalUnits(p.effectUnit),resource:Number(p.resource),resourceUnit:canonicalUnits(p.resourceUnit),verified:p.verification?.status==='verified'||p.verified===true,uncertainty:p.uncertainty||null,sourceIds:[...new Set((p.sourceIds||[]).map(text).filter(Boolean))]}));const valid=params.filter(p=>finite(p.effect)&&positive(p.resource)&&p.effectUnit&&p.resourceUnit&&p.verified);const complete=params.length>0&&valid.length===params.length;return {parameters:params,validCount:valid.length,hasValidParameter:complete,unitsComplete:complete&&valid.every(p=>p.effectUnit&&p.resourceUnit),independentlySourced:complete&&valid.every(p=>p.sourceIds.length>=2)};}
function buildStatusQuo(input={}){const description=text(input.description)||'Continue the current approach until an admissible alternative is established.';return {explicit:true,id:text(input.id)||'status-quo',description,effect:finite(input.effect)?Number(input.effect):null,effectUnit:canonicalUnits(input.effectUnit),resource:finite(input.resource)?Number(input.resource):null,resourceUnit:canonicalUnits(input.resourceUnit),observed:input.observed===true};}
function recommendationGate(candidateRaw,context={}){const candidate=normalizeCandidate(candidateRaw);const evidence=evidenceState(candidate);const parameters=parameterState(candidate);const statusQuo=context.statusQuo||buildStatusQuo();const rawEffect=finite(candidateRaw?.effect)?Number(candidateRaw.effect):null;const rawResource=finite(candidateRaw?.resource)?Number(candidateRaw.resource):null;const p0=parameters.parameters[0];const conflicts=[];if(rawEffect!==null&&p0&&finite(p0.effect)&&rawEffect!==Number(p0.effect))conflicts.push('effect-parameter-conflict');if(rawResource!==null&&p0&&finite(p0.resource)&&rawResource!==Number(p0.resource))conflicts.push('resource-parameter-conflict');const gates={A_problemDefined:text(context.problem).length>0,B_statusQuoExplicit:statusQuo.explicit===true,C_independentEvidence:evidence.hasIndependentCausalEvidence,D_verifiedParameter:parameters.hasValidParameter&&parameters.independentlySourced,E_resourceEffectUnits:parameters.unitsComplete};const blockedReasons=[...conflicts];if(!gates.A_problemDefined)blockedReasons.push('problem-not-defined');if(!gates.B_statusQuoExplicit)blockedReasons.push('status-quo-missing');if(!gates.C_independentEvidence)blockedReasons.push('independent-causal-evidence-missing');if(!gates.D_verifiedParameter)blockedReasons.push('independently-verified-parameter-missing');if(!gates.E_resourceEffectUnits)blockedReasons.push('resource-effect-units-missing');if(candidate.discoveryOnly||candidate.leadOnly)blockedReasons.push('discovery-lead-not-recommendation');if(candidate.effectsImported||evidence.importedEffectCount>0)blockedReasons.push('imported-effect-blocked');return {allowed:blockedReasons.length===0,candidateId:candidate.id,gates,blockedReasons:[...new Set(blockedReasons)],evidence,parameters,statusQuo};}
function sensitivity(candidateRaw,scenarios=[]){const candidate=normalizeCandidate(candidateRaw);const params=parameterState(candidate).parameters.filter(p=>finite(p.effect)&&positive(p.resource));if(!params.length)return {defined:false,flips:[],reason:'no-valid-parameter'};const base=params[0];const rows=scenarios.map((s,index)=>{const effect=finite(s.effect)?Number(s.effect):base.effect;const resource=positive(s.resource)?Number(s.resource):base.resource;const ratio=effect/resource;const baselineRatio=finite(s.baselineRatio)?Number(s.baselineRatio):0;return {id:text(s.id)||`scenario-${index+1}`,effect,resource,ratio,recommendationEligible:s.recommendationEligible===true,flip:(ratio>baselineRatio)!==(base.effect/base.resource>baselineRatio)};});return {defined:true,baseRatio:base.effect/base.resource,flips:rows.filter(r=>r.flip),scenarios:rows};}
function safeNonNegative(x){const n=Number(x);return Number.isFinite(n)&&n>0?n:0;}function estimateVOI(input={}){const uncertainty=safeNonNegative(input.uncertainty),decisionGap=safeNonNegative(input.decisionGap),cost=safeNonNegative(input.researchCost),value=uncertainty*decisionGap;return {valueOfInformation:value,researchCost:cost,netValue:value-cost,priority:value>cost?'high':value>0?'medium':'low'};}
function decisionReadiness(problem,candidates,context={}){const statusQuo=buildStatusQuo(context.statusQuo||{});const rows=(candidates||[]).map(c=>recommendationGate(c,{problem,statusQuo}));const eligible=rows.filter(r=>r.allowed);return {problem:text(problem),statusQuo,candidateCount:rows.length,eligibleCount:eligible.length,recommendation:eligible.length?{allowed:true,candidateId:eligible[0].candidateId,reason:'all-gates-passed'}:{allowed:false,reason:'no-candidate-passed-all-gates'},candidates:rows,nextActions:eligible.length?['run sensitivity','calculate opportunity cost','persist immutable decision artifact']:['close evidence gaps','verify parameters independently','run sensitivity/VOI before recommendation']};}
function canonicalize(value){if(Array.isArray(value))return value.map(canonicalize);if(value&&typeof value==='object')return Object.keys(value).sort().reduce((o,k)=>{o[k]=canonicalize(value[k]);return o;},{});return value;}
async function auditHash(value){const payload=JSON.stringify(canonicalize(value));if(typeof crypto!=='undefined'&&crypto.subtle){const bytes=new TextEncoder().encode(payload);const digest=await crypto.subtle.digest('SHA-256',bytes);return [...new Uint8Array(digest)].map(b=>b.toString(16).padStart(2,'0')).join('');}let h=2166136261;for(let i=0;i<payload.length;i++)h=Math.imul(h^payload.charCodeAt(i),16777619);return(h>>>0).toString(16).padStart(8,'0');}
module.exports={normalizeCandidate,classifyEvidence,evidenceState,parameterState,buildStatusQuo,recommendationGate,sensitivity,estimateVOI,decisionReadiness,auditHash};
