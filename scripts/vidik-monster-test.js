#!/usr/bin/env node
'use strict';
const assert = require('assert');
const { runAll, runCity } = require('./municipal-production-decision-run');
const { runCanonicalCity, withResourceEnvelope } = require('./municipal-canonical-decision-run');
const { evaluateResourceOptimization, validateModel } = require('../js/vidik-resource-optimization');
const { CANONICAL_18, validateDecisionObject } = require('../js/vidik-architecture-contract');
const HOUSING = { capacityPerCad: 0.000001, activityPerCapacity: 100, effectPerActivity: 0.002, objectiveMetric: 'common_decision_outcome', capacityUnit: 'housing_slots', activityUnit: 'placements', effectUnit: 'common_decision_outcome', evidenceIds: ['resource-capacity:housing','resource-activity:housing','resource-effect:housing'], uncertainty: { low: 0.30, high: 0.50 } };
const ASE = { capacityPerCad: 0.000002, activityPerCapacity: 80, effectPerActivity: 0.004, objectiveMetric: 'common_decision_outcome', capacityUnit: 'enforcement_slots', activityUnit: 'speed_interventions', effectUnit: 'common_decision_outcome', evidenceIds: ['resource-capacity:ase','resource-activity:ase','resource-effect:ase'], uncertainty: { low: 0.20, high: 0.40 } };
const CITIES = ['Ottawa','Toronto','Melbourne'];
const BUDGETS = [1,1000,100000,1000000,5000000,25000000,100000000];
const RESULTS = [];
function pass(id, detail) { RESULTS.push({id,status:'PASS',detail}); }
function expectedFailure(id, fn, pattern) { assert.throws(fn, pattern); pass(id,`failed closed as expected: ${pattern}`); }
async function main() {
  const baseline=await runAll();
  assert.deepStrictEqual(baseline.cities.map(x=>x.city),CITIES); assert.strictEqual(baseline.cities.find(x=>x.city==='Ottawa').recommendation,'housing'); assert.strictEqual(baseline.cities.find(x=>x.city==='Toronto').recommendation,'housing'); assert.strictEqual(baseline.cities.find(x=>x.city==='Melbourne').decisionState,'BLOCKED');
  assert.strictEqual(baseline.cities.find(x=>x.city==='Ottawa').observedContext.value,2952); assert.strictEqual(baseline.cities.find(x=>x.city==='Toronto').observedContext.value,15400); assert.strictEqual(baseline.cities.find(x=>x.city==='Melbourne').observedContext.value,147); pass('municipal-original-production','Ottawa/Toronto recommend; Melbourne blocks; live values 2952/15400/147');
  for(const city of CITIES){const d=await runCanonicalCity(city,withResourceEnvelope({},5000000)); assert.strictEqual(Object.keys(d).length,CANONICAL_18.length); assert.strictEqual(validateDecisionObject(d).valid,true); assert.strictEqual(d.causalProductionModel.observedMunicipalDataIsNotCausal,true); assert.strictEqual(d.integrity.syntheticEvidenceExcluded,true); pass(`canonical-${city}`,'18-part canonical object validates and preserves observed-vs-causal boundary');}
  for(const amount of BUDGETS){const r=await runAll({...withResourceEnvelope({},amount),resourceModels:{housing:HOUSING,ase:ASE}}); for(const city of ['Ottawa','Toronto']){const d=r.cities.find(x=>x.city===city); assert.strictEqual(d.resourceEnvelope.marginalUnit.amount,amount); assert.strictEqual(d.optimization.status,'OPTIMIZED'); assert.strictEqual(d.optimization.allocation.intervention,'ase');} assert.strictEqual(r.cities.find(x=>x.city==='Melbourne').optimization.status,'BLOCKED'); pass(`city-budget-${amount}`,'budget preserved; highest evidenced marginal return selected; Melbourne remains blocked');}
  for(const amount of BUDGETS){const r=await runAll(withResourceEnvelope({},amount)); for(const city of r.cities) assert.notStrictEqual(city.optimization.status,'OPTIMIZED'); pass(`budget-without-model-${amount}`,'money alone cannot manufacture a resource-to-outcome chain');}
  const flip=await runCity('Ottawa',{scenarioEvidence:{Ottawa:{ase:{admissible:true,evidence:{id:'ase-flip',estimate:.80,unit:'scenario effect',uncertainty:{low:.70,high:.90},jurisdiction:'Ottawa',mode:'scenario'}}}}}); assert.strictEqual(flip.recommendation,'ase'); pass('recommendation-flip','explicit competing evidence flips Ottawa recommendation');
  const locked=await runCity('Melbourne'); assert.strictEqual(locked.decisionState,'BLOCKED'); const unlocked=await runCity('Melbourne',{scenarioEvidence:{Melbourne:{housing:{admissible:true,evidence:{id:'housing-au',estimate:.42,unit:'absolute stable-housing probability difference',uncertainty:{low:.30,high:.54},jurisdiction:'Australia',mode:'site-supported-scenario'}}}}}); assert.strictEqual(unlocked.recommendation,'housing'); pass('transportability-boundary','Canada evidence stays blocked in Melbourne until explicit Australian evidence is supplied');
  const close=await runCity('Ottawa',{outcome:{predicted:.42,observed:.40,checkpoint:'6-month',provenance:'monster-test'}}); const drift=await runCity('Ottawa',{outcome:{predicted:.42,observed:.30,checkpoint:'6-month',provenance:'monster-test'}}); assert.strictEqual(close.learning.drift.detected,false); assert.strictEqual(drift.learning.drift.detected,true); assert.strictEqual(drift.learning.recalibration.application,'EXPLICIT_PARAMETER_MAPPING'); pass('learning-drift-recalibration','close outcome does not drift; material error raises drift; recalibration is explicit');
  const complete=evaluateResourceOptimization({marginalUnit:{amount:5000000,unit:'CAD'}},[{id:'housing',name:'Housing',status:'ADMISSIBLE'},{id:'ase',name:'ASE',status:'ADMISSIBLE'}],{housing:HOUSING,ase:ASE}); assert.strictEqual(complete.status,'OPTIMIZED'); assert.strictEqual(complete.allocation.intervention,'ase'); assert.strictEqual(complete.opportunityCost.foregoneIntervention,'housing');
  const incomplete=validateModel({id:'housing'},{...HOUSING,effectPerActivity:undefined}); assert.strictEqual(incomplete.admissible,false); const mismatch=evaluateResourceOptimization({marginalUnit:{amount:5000000,unit:'CAD'}},[{id:'housing',name:'Housing',status:'ADMISSIBLE'},{id:'ase',name:'ASE',status:'ADMISSIBLE'}],{housing:HOUSING,ase:{...ASE,objectiveMetric:'serious_harm_events'}}); assert.strictEqual(mismatch.status,'BLOCKED'); pass('resource-chain-boundaries','complete chain optimizes; missing step blocks; incomparable objectives block');
  expectedFailure('invalid-budget-zero',()=>withResourceEnvelope({},0),/positive-finite/); expectedFailure('invalid-budget-negative',()=>withResourceEnvelope({},-1),/positive-finite/); expectedFailure('invalid-budget-infinity',()=>withResourceEnvelope({},Infinity),/positive-finite/); expectedFailure('invalid-budget-nan',()=>withResourceEnvelope({},NaN),/positive-finite/);
  for(const [id,model] of [['nan',{...HOUSING,capacityPerCad:NaN}],['negative',{...HOUSING,effectPerActivity:-1}],['zero',{...HOUSING,activityPerCapacity:0}],['missing-lineage',{...HOUSING,evidenceIds:['one','two']}],['bad-uncertainty',{...HOUSING,uncertainty:{low:.8,high:.2}}]]){assert.strictEqual(validateModel({id:'housing'},model).admissible,false); pass(`invalid-resource-${id}`,'resource model rejected');}
  assert.strictEqual(evaluateResourceOptimization({marginalUnit:{amount:1,unit:'CAD'}},[{id:'housing',name:'Housing',status:'ADMISSIBLE'}],{}).status,'BLOCKED'); assert.strictEqual(evaluateResourceOptimization({marginalUnit:{amount:1,unit:'CAD'}},[{id:'housing',name:'Housing',status:'BLOCKED'}],{housing:HOUSING}).status,'BLOCKED'); pass('adversarial-resource-validation','invalid models, missing models and inadmissible interventions fail closed');
  const denied=await runCity('Ottawa',{scenarioEvidence:{Ottawa:{housing:{admissible:false,failure:'evidence-withdrawn'}}}}); assert.strictEqual(denied.decisionState,'BLOCKED'); assert.strictEqual(denied.audit.failureClosed,true); pass('evidence-withdrawal','explicit withdrawal blocks recommendation');
  const businessModels={safetyProgram:{...HOUSING,objectiveMetric:'business_risk_reduction',evidenceIds:['business-capacity','business-activity','business-effect']},lossPrevention:{...ASE,objectiveMetric:'business_risk_reduction',evidenceIds:['loss-capacity','loss-activity','loss-effect']}}; const business=evaluateResourceOptimization({marginalUnit:{amount:250000,unit:'CAD'}},[{id:'safetyProgram',name:'Safety program',status:'ADMISSIBLE'},{id:'lossPrevention',name:'Loss prevention',status:'ADMISSIBLE'}],businessModels); assert.strictEqual(business.status,'OPTIMIZED'); assert.strictEqual(business.objectiveMetric,'business_risk_reduction'); pass('business-resource-allocation','non-municipal objective works only with explicit comparable evidence/model');
  const developmentModels={siteA:{...HOUSING,objectiveMetric:'development_feasibility',evidenceIds:['siteA-capacity','siteA-activity','siteA-effect']},siteB:{...ASE,objectiveMetric:'development_feasibility',evidenceIds:['siteB-capacity','siteB-activity','siteB-effect']}}; const development=evaluateResourceOptimization({marginalUnit:{amount:1000000,unit:'CAD'}},[{id:'siteA',name:'Site A',status:'ADMISSIBLE'},{id:'siteB',name:'Site B',status:'ADMISSIBLE'}],developmentModels); assert.strictEqual(development.status,'OPTIMIZED'); assert.strictEqual(development.objectiveMetric,'development_feasibility'); pass('realtor-developer-resource-allocation','site alternatives compare when objective, chain and evidence are explicit');
  assert.strictEqual((await runCity('Ottawa')).objective,'verified-outcome-improvement'); pass('domain-wrapper-boundary','municipal wrapper remains municipal; generic domain optimization is engine-level');
  const scale1=evaluateResourceOptimization({marginalUnit:{amount:100000,unit:'CAD'}},[{id:'housing',name:'Housing',status:'ADMISSIBLE'},{id:'ase',name:'ASE',status:'ADMISSIBLE'}],{housing:HOUSING,ase:ASE}); const scale10=evaluateResourceOptimization({marginalUnit:{amount:1000000,unit:'CAD'}},[{id:'housing',name:'Housing',status:'ADMISSIBLE'},{id:'ase',name:'ASE',status:'ADMISSIBLE'}],{housing:HOUSING,ase:ASE}); assert.strictEqual(scale1.allocation.intervention,scale10.allocation.intervention); assert.strictEqual(scale10.candidates.find(x=>x.id==='ase').translation.outcome.expectedIncrement,scale1.candidates.find(x=>x.id==='ase').translation.outcome.expectedIncrement*10); pass('resource-scale-invariance','10x resource scales modeled outcome 10x without changing ranking');
  const tie=evaluateResourceOptimization({marginalUnit:{amount:100000,unit:'CAD'}},[{id:'b',name:'B',status:'ADMISSIBLE'},{id:'a',name:'A',status:'ADMISSIBLE'}],{a:{...HOUSING,effectPerActivity:.002},b:{...HOUSING,effectPerActivity:.002}}); assert.strictEqual(tie.allocation.intervention,'a'); assert.strictEqual(tie.opportunityCost.foregoneIntervention,'b'); pass('deterministic-tie-break','equal marginal returns resolve deterministically and expose opportunity cost');
  assert.strictEqual(evaluateResourceOptimization({marginalUnit:{amount:100000,unit:'CAD'}},[{id:'housing',name:'Housing',status:'ADMISSIBLE'}],{housing:{...HOUSING,evidenceIds:[]}}).status,'BLOCKED'); pass('evidence-lineage-gate','resource optimization blocks without minimum evidence lineage');
  const canonical=await runCanonicalCity('Ottawa',withResourceEnvelope({outcome:{predicted:.42,observed:.40,provenance:'monster-test'}},5000000)); assert.strictEqual(canonical.counterfactualVault.status,'RECORDED'); assert.strictEqual(canonical.governanceOverridesAudit.humanOverride,null); assert.strictEqual(canonical.outcomeLearningCheckpoints.recalibrationMutatesParametersAutomatically,false); assert.ok(canonical.integrity.evidenceHash); pass('counterfactual-audit-learning-integrity','counterfactual, lineage hash, learning checkpoint and no-auto-mutation invariants hold');
  const summary={schemaVersion:'vidik.monster-test.v2',passed:RESULTS.every(x=>x.status==='PASS'),testCount:RESULTS.length,domains:['municipal','canonical','city-budget','resource-optimization','transportability','learning','business','realtor-developer','adversarial','integrity'],results:RESULTS}; process.stdout.write(JSON.stringify(summary,null,2)+'\n'); if(!summary.passed) process.exitCode=1;
}
if(require.main===module) main().catch(error=>{console.error(error.stack||error);process.exitCode=1;}); module.exports={main};
