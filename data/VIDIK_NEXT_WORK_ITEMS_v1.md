# VIDIK Next Work Items v1

1. Build the customer-facing implementation of the canonical decision package in the existing HTML product.
2. Wire benchmark instrumentation into real decision runs rather than fixture-only measurements.
3. Implement adapter configuration for Ottawa first, then prove the same contract with Toronto and Melbourne.
4. Prepare frozen October CI candidate and execute only after local gates pass.
5. Prepare a real municipal pilot protocol and identify a decision class with accessible operational data.
6. Use the RC4 acquisition priority map to scope the first data request only after the exact frozen request package is confirmed.
